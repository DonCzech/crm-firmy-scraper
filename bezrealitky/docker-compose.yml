version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: bezrealitky_pg
    restart: unless-stopped
    environment:
      POSTGRES_USER: bezrealitky
      POSTGRES_PASSWORD: bezrealitky
      POSTGRES_DB: bezrealitky
    ports:
      - '5432:5432'
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: bezrealitky_redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

  minio:
    image: minio/minio:latest
    container_name: bezrealitky_minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data

volumes:
  pg_data:
  redis_data:
  minio_data:
